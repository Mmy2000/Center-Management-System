# Free-tier deploy: base plus the Excel writer, and nothing else.
# No psycopg / gunicorn / redis / whitenoise — PythonAnywhere provides the
# server, and the free tier has neither PostgreSQL nor Redis.
-r base.txt
openpyxl==3.1.5
